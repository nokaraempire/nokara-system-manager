<UserControl x:Class="NokaraSystemManager.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Procesos y prioridades" Style="{StaticResource PageTitleTextStyle}" />
        <TextBlock Text="Detecta juegos, launchers, streaming, navegadores y procesos pesados. La prioridad Realtime está bloqueada." Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,12" />

        <WrapPanel>
            <Border Style="{StaticResource CardBorderStyle}" Width="210" Padding="14">
                <StackPanel>
                    <TextBlock Text="Total detectado" Style="{StaticResource MetaTextStyle}" />
                    <TextBlock Text="{Binding TotalProcesses}" FontSize="24" FontWeight="SemiBold" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="210" Padding="14">
                <StackPanel>
                    <TextBlock Text="Procesos pesados" Style="{StaticResource MetaTextStyle}" />
                    <TextBlock Text="{Binding HeavyProcessCount}" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource WarningBrush}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="210" Padding="14">
                <StackPanel>
                    <TextBlock Text="Juegos detectados" Style="{StaticResource MetaTextStyle}" />
                    <TextBlock Text="{Binding GameProcessCount}" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="210" Padding="14">
                <StackPanel>
                    <TextBlock Text="Streaming detectado" Style="{StaticResource MetaTextStyle}" />
                    <TextBlock Text="{Binding StreamingProcessCount}" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource CyanAccentBrush}" />
                </StackPanel>
            </Border>
        </WrapPanel>

        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="980">
            <StackPanel>
                <TextBlock Text="Control seguro" Style="{StaticResource SectionTitleTextStyle}" />
                <TextBlock Text="{Binding Status}" Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,8" />
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ListBox ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" Background="{StaticResource TransparentBrush}" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}">
                                    <Setter Property="Padding" Value="9,4" />
                                    <Setter Property="Margin" Value="0,0,6,6" />
                                    <Setter Property="MinHeight" Value="30" />
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <Border x:Name="Root" Background="{StaticResource SurfaceHighBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="11" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentSoftBrush}" />
                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Root" Property="Background" Value="{StaticResource SelectionBrush}" />
                                                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentSoftBrush}" />
                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <WrapPanel Grid.Column="1" VerticalAlignment="Top" Margin="12,0,0,0">
                        <Button Content="Actualizar detección" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                        <ComboBox ItemsSource="{Binding PriorityOptions}" SelectedItem="{Binding SelectedPriority}" Width="150" />
                        <Button Content="Aplicar prioridad" Command="{Binding ApplyPriorityCommand}" />
                    </WrapPanel>
                </Grid>
                <TextBlock Text="Usá AboveNormal para OBS/Discord si hace falta, High solo para juego seleccionado. Esta versión no cierra procesos automáticamente." Style="{StaticResource MetaTextStyle}" Margin="0,0,0,10" />
                <DataGrid ItemsSource="{Binding FilteredProcesses}" SelectedItem="{Binding SelectedProcess}" MaxHeight="420">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Proceso" Binding="{Binding DisplayName}" Width="135" />
                        <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="48" />
                        <DataGridTextColumn Header="Categoría" Binding="{Binding Category}" Width="86" />
                        <DataGridTextColumn Header="RAM" Binding="{Binding MemoryMB}" Width="62" />
                        <DataGridTextColumn Header="Prioridad" Binding="{Binding Priority}" Width="82" />
                        <DataGridCheckBoxColumn Header="Pesado" Binding="{Binding IsHighMemory}" Width="58" />
                        <DataGridTextColumn Header="Recomendación" Binding="{Binding RecommendedAction}" Width="180" />
                        <DataGridTextColumn Header="Notas" Binding="{Binding Notes}" Width="*" MinWidth="110" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
