<UserControl x:Class="NokaraSystemManager.Views.BackupRestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Backups y restauración" Style="{StaticResource PageTitleTextStyle}" />
        <TextBlock Text="{Binding Status}" Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,12" />
        <WrapPanel Margin="0,0,0,8">
            <Button Content="Actualizar" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButtonStyle}" />
            <Button Content="Crear punto restauración" Command="{Binding CreateRestorePointCommand}" />
            <Button Content="Revertir todo" Command="{Binding RestoreAllCommand}" Style="{StaticResource SecondaryButtonStyle}" />
            <Button Content="Abrir backups" Command="{Binding OpenBackupsFolderCommand}" Style="{StaticResource SecondaryButtonStyle}" />
        </WrapPanel>
        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="980">
            <TextBlock Text="Si no hay filas, todavía no existen cambios reversibles. Cada acción que toca registro/DNS/energía guarda un backup antes de modificar." Style="{StaticResource SecondaryTextStyle}" />
        </Border>

        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="980">
            <StackPanel>
                <TextBlock Text="Registro" Style="{StaticResource SectionTitleTextStyle}" />
                <Button Content="Restaurar seleccionado" Command="{Binding RestoreRegistryCommand}" Style="{StaticResource SmallButtonStyle}" HorizontalAlignment="Left" />
                <DataGrid ItemsSource="{Binding RegistryBackups}" SelectedItem="{Binding SelectedRegistryBackup}" MaxHeight="190">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Timestamp}" Width="150" />
                        <DataGridTextColumn Header="Modulo" Binding="{Binding Module}" Width="100" />
                        <DataGridTextColumn Header="Acción" Binding="{Binding ActionName}" Width="180" />
                        <DataGridTextColumn Header="Destino" Binding="{Binding Target}" Width="*" />
                        <DataGridCheckBoxColumn Header="Rest." Binding="{Binding IsRestored}" Width="60" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="980">
            <StackPanel>
                <TextBlock Text="DNS" Style="{StaticResource SectionTitleTextStyle}" />
                <Button Content="Restaurar DNS seleccionado" Command="{Binding RestoreDnsCommand}" Style="{StaticResource SmallButtonStyle}" HorizontalAlignment="Left" />
                <DataGrid ItemsSource="{Binding DnsBackups}" SelectedItem="{Binding SelectedDnsBackup}" MaxHeight="160">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Timestamp}" Width="150" />
                        <DataGridTextColumn Header="Adaptador" Binding="{Binding AdapterName}" Width="220" />
                        <DataGridTextColumn Header="DNS anterior" Binding="{Binding PreviousDnsDisplay}" Width="*" />
                        <DataGridCheckBoxColumn Header="Rest." Binding="{Binding IsRestored}" Width="60" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="980">
            <StackPanel>
                <TextBlock Text="Plan de energía" Style="{StaticResource SectionTitleTextStyle}" />
                <Button Content="Restaurar plan seleccionado" Command="{Binding RestorePowerPlanCommand}" Style="{StaticResource SmallButtonStyle}" HorizontalAlignment="Left" />
                <DataGrid ItemsSource="{Binding PowerPlanBackups}" SelectedItem="{Binding SelectedPowerPlanBackup}" MaxHeight="150">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Timestamp}" Width="150" />
                        <DataGridTextColumn Header="Anterior" Binding="{Binding PreviousPlanName}" Width="*" />
                        <DataGridTextColumn Header="Nuevo" Binding="{Binding NewPlanName}" Width="160" />
                        <DataGridCheckBoxColumn Header="Rest." Binding="{Binding IsRestored}" Width="60" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="980">
            <StackPanel>
                <TextBlock Text="Historial" Style="{StaticResource SectionTitleTextStyle}" />
                <DataGrid ItemsSource="{Binding History}" MaxHeight="230">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Timestamp}" Width="150" />
                        <DataGridTextColumn Header="Modulo" Binding="{Binding Module}" Width="110" />
                        <DataGridTextColumn Header="Acción" Binding="{Binding ActionName}" Width="220" />
                        <DataGridCheckBoxColumn Header="OK" Binding="{Binding Success}" Width="55" />
                        <DataGridTextColumn Header="Mensaje" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
